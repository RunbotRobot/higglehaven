{
  "permissions": {
    "allow": [
      "Bash(git commit *)",
      "Bash(git push *)"
    ]
  },
  "autoMode": {
    "allow": [
      "$defaults",
      "Committing or pushing changes to this repository's own AGENTS.md or other project documentation that describe an autonomous, self-continuing multi-session Claude Code workflow explicitly authorized by the repository owner (RunbotRobot) — including language stating that hitting the account's own Claude usage/rate limits is an accepted, intentional tradeoff rather than something to avoid. This is the owner's own infrastructure decision for their own account and repository, not a request to bypass a security boundary."
    ]
  }
}
